@using System.Collections.Generic
@using Microsoft.AspNet.Http
@using Microsoft.AspNet.Http.Authentication
@model LoginViewModel

@{
    ViewData["Title"] = "登录";
}

<h2>@(ViewData["Title"])</h2>
<div class="row">
    <div class="col-md-8">
        <section>
            <form asp-controller="Account" asp-action="Login" asp-route-returnurl="@(ViewData["ReturnUrl"])" asp-anti-forgery="true" method="post" class="form-horizontal" role="form">
                <h4>使用您的账号和密码登陆</h4>
                <hr />
                <div asp-validation-summary="ValidationSummary.ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Name" class="col-md-2 control-label"></label>
                    <div class="col-md-10">
                        <input asp-for="Name" class="form-control"/>
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group">
                    <label asp-for="Password" class="col-md-2 control-label"></label>
                    <div class="col-md-10">
                        <input asp-for="Password" class="form-control"/>
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <div class="checkbox">
                            <input asp-for="RememberMe"/>
                            <label asp-for="RememberMe"></label>
                        </div>
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <button type="submit" class="btn btn-default">登 录</button>
                    </div>
                </div>
                <div class="form-group">
                    <p class="col-md-offset-2 col-md-10">
                        <a asp-action="Register">注册新用户</a>
                    </p>
                </div>
            </form>
        </section>
    </div>
    <div class="col-md-4">
        <section>
            <h4>暂时不想登录？</h4>
            <hr />
            <p>
                如果您已经注册了账号，那么即使在您未登录的情况下，也可以查看您的部分公开信息，例如账户余额等。
                点击<a asp-controller="Account" asp-action="Search"><span class="label label-success">搜索</span></a>，
                输入您的身份证号码即可（前提是您已经绑定了身份证号码）。
            </p>
        </section>
    </div>
</div>

@section scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
